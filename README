

Type in the following command ‘ wget --no-cache https://raw.githubusercontent.com/chupix13/alvskaprox/master/setup.sh ‘ & execute

Type in the following command ‘ ls ‘ & execute. “ setup.sh “ should show up, meaning the script has been downloaded on the VPS

Type in the following command ‘ sudo bash setup.sh ‘ & execute. This should take up to like 5 minutes.

